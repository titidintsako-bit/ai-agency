"""
agents/

One subdirectory per deployed client agent.

Each client folder contains:
    agent.py   - The agent instance using the core BaseAgent framework
    tools.py   - Client-specific tools (appointment booking, FAQ lookup, etc.)

Current clients:
    smilecare/ - Demo dental clinic (SmileCare Dental)
"""
